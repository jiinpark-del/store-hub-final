# All in One Store Hub - 현황 분석 보고서

**작성일**: 2026-08-17  
**프로젝트**: All in One Store Hub  
**기술 스택**: React, Node.js, PostgreSQL  

---

## Executive Summary

All in One Store Hub는 **가게 매니저의 업무 자동화**와 **본사 관리자의 데이터 무결성**을 동시에 해결하는 통합 솔루션입니다. OCR 기반 인보이스 처리와 안전한 세일즈 입력 시스템이 핵심입니다.

**현재 상태**: 개념 기획 단계  
**주요 성공 요소**: OCR 정확도, 데이터 무결성, 사용자 경험  
**임계 위험**: AI 모델 의존성, 대용량 이미지 처리, 데이터 동기화  

---

## 1. 시스템 아키텍처 분석

### 1.1 핵심 모듈 구성

```
┌─────────────────────────────────────────┐
│         Manager App Layer               │
│  (React Native/PWA on Mobile/Tablet)    │
└──────────┬──────────────────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐  ┌──────────────┐
│Sales    │  │Invoice Upload│
│Form     │  │+ OCR Engine  │
└────┬────┘  └──────┬───────┘
     │              │
     └──────┬───────┘
            ▼
    ┌──────────────────┐
    │  Node.js Backend │
    │ (API + Workers)  │
    └────┬─────────────┘
         │
         ▼
    ┌──────────────────┐
    │  PostgreSQL DB   │
    │ (Transaction Safe)
    └────┬─────────────┘
         │
         ▼
    ┌──────────────────────┐
    │  Admin Dashboard     │
    │  (React Web App)     │
    └──────────────────────┘
```

### 1.2 데이터 흐름

**Sales 입력 흐름:**
1. 매니저 앱 → Sales Form 입력
2. 즉시 유효성 검증 (client-side)
3. Node.js API → PostgreSQL 저장 (transaction)
4. 대시보드 실시간 반영 (WebSocket/polling)

**OCR 인보이스 흐름:**
1. 이미지 업로드 → S3/Cloud Storage
2. AI OCR 엔진 처리 (async)
3. 추출 데이터 (업체명, 인보이스 번호, 금액, 날짜)
4. 매니저 검증 화면 제시
5. 승인 후 DB 저장 + 자동 분류

**Reconciliation 흐름:**
1. 본사 → Statement 파일 업로드 (Excel/CSV)
2. 파싱 후 OCR 데이터와 매칭
3. 불일치 항목 자동 감지 (빨간불)
4. 지점/업체/날짜별 차이 보고

---

## 2. 현재 성능 병목 지점 (Critical)

### 2.1 OCR 처리 성능

**문제점:**
- 이미지 대용량 → 업로드 시간 지연
- AI 모델 응답 시간 불안정 (3~10초 변동)
- 배치 처리 시 큐 대기 시간 누적
- 인식 정확도 편차 (문서 품질에 따라 60~95%)

**영향도**: 🔴 **Critical** (매니저 UX 저하)

**개선 방안**:
```
우선순위 1: 이미지 전처리 + 최적화
  - 클라이언트 사이드 압축 (jpeg 80%, 1080p 이하)
  - 회전 감지 및 자동 보정
  - 예상 처리 시간 단축: 8~10초 → 3~5초

우선순위 2: 비동기 큐 시스템
  - Redis 기반 Job Queue (Bull.js)
  - 병렬 처리 (워커 스케일링)
  - 우선순위 지정 (중요 지점 우선)

우선순위 3: 하이브리드 OCR
  - Google Vision API (신뢰도 높음)
  - Tesseract (로컬 빠름)
  - 동적 선택 로직
```

### 2.2 데이터 동기화 문제

**문제점:**
- 다중 지점 동시 입력 시 네트워크 지연
- 오프라인 모드 미지원 → 네트워크 불안정 지역 문제
- 중복 제출 방지 로직 없음
- 여러 매니저가 같은 데이터 수정 시 충돌

**영향도**: 🟠 **High** (데이터 무결성)

**개선 방안**:
```
1. 낙관적 업데이트 (Optimistic Update)
   - 로컬에서 즉시 반영, 서버 응답 대기
   - 실패 시 롤백

2. 충돌 해결 전략
   - Last-Write-Wins (타임스탐프 기반)
   - 또는 Operation Transform (OT)
   - 또는 CRDT (Conflict-free Replicated Data Type)

3. 오프라인 지원
   - IndexedDB/SQLite 로컬 캐시
   - 네트워크 복구 시 자동 동기화
   - Sync 상태 UI 피드백
```

### 2.3 Statement Reconciliation 성능

**문제점:**
- 대량 인보이스 vs Statement 비교 시 O(n²) 복잡도
- 정확한 매칭 로직 (번호, 금액, 공급사)
- 환율 변동 / 세금 처리 미고려
- 월말 피크 시 대시보드 응답 지연

**영향도**: 🟠 **High** (관리자 업무)

**개선 방안**:
```
1. 인덱싱 + DB 최적화
   - Supplier ID + Invoice Number 복합 인덱스
   - 날짜 범위 조회 성능 튜닝
   - 조회 시간: 수 초 → 밀리초

2. 백그라운드 재조정
   - Statement 업로드 시 async Job
   - 결과는 캐시 + 실시간 업데이트

3. 스마트 매칭 알고리즘
   - 우선: 발주처 + 인보이스 번호 (100% 정확)
   - 차선: 발주처 + 금액 (오차 ±1%)
   - 신뢰도 스코어 표시
```

---

## 3. 위험도 평가

### 3.1 기술적 위험 (Technical Risk)

| 위험 항목 | 심각도 | 원인 | 완화 전략 |
|---------|-------|------|---------|
| **OCR 정확도 편차** | 🔴 High | 문서 품질 / 조명 | 다중 모델 앙상블, 사용자 피드백 학습 |
| **확장성 (Scalability)** | 🟠 Medium | 다중 지점 + 대용량 이미지 | 마이크로서비스, CDN, DB 샤딩 계획 |
| **AI 모델 비용** | 🟠 Medium | Vision API 호출 과다 | 캐싱, 배치 처리, 로컬 모델 검토 |
| **데이터 마이그레이션** | 🟠 Medium | 기존 Google Sheets → DB | 검증 프로세스, 롤백 계획 필수 |
| **보안 (Sensitive Data)** | 🔴 High | 금융 데이터 + 이미지 | 암호화, RBAC, 감사 로그 |

### 3.2 운영 위험 (Operational Risk)

| 위험 항목 | 심각도 | 영향 | 대비책 |
|---------|-------|------|-------|
| **API Downtime** | 🟠 Medium | 매니저 입력 불가 | 오프라인 모드, 재시도 로직 |
| **OCR 서비스 장애** | 🟠 Medium | 인보이스 처리 정지 | Fallback 모델, Manual 입력 옵션 |
| **데이터 손실** | 🔴 High | 재무 기록 유실 | 매일 백업, Point-in-time 복구 |
| **성능 저하 (달마다)** | 🟠 Medium | Reconciliation 지연 | 모니터링 + 자동 알람 |

### 3.3 사용자 위험 (UX Risk)

| 위험 항목 | 영향 | 해결 |
|---------|------|------|
| **높은 러닝커브** | 매니저 채택 지연 | 명확한 온보딩, 튜토리얼 비디오 |
| **OCR 오류 검증 번거로움** | 사용자 이탈 | 자동 검증 임계값, 빠른 수정 UX |
| **네트워크 불안정성** | 입력 실패 경험 | 오프라인 + Auto-sync 지원 |

---

## 4. 핵심 성공 지표 (KPI)

### 4.1 기술 지표

```
1. OCR 정확도
   목표: ≥92% (숫자 필드), ≥88% (텍스트 필드)
   측정: 수동 검증된 샘플 기반
   주기: 주 단위

2. 처리 시간
   - 이미지 업로드 → OCR 결과: ≤5초 (p95)
   - Statement 업로드 → Reconciliation 완료: ≤30초
   측정: 백엔드 로그 분석

3. 가용성
   목표: 99.5% uptime
   측정: Monitoring alerts

4. 데이터 무결성
   - 중복 입력: 0
   - 손실 레코드: 0
   측정: 주간 감사
```

### 4.2 비즈니스 지표

```
1. 매니저 만족도
   - 입력 시간 단축 (목표: 50% 단축)
   - 타이핑 오류 감소 (목표: 90% 감소)

2. 관리자 효율성
   - Statement 처리 시간 (수동 vs 자동)
   - 미매칭 인보이스 발견 속도

3. 채택율
   - 활성 지점 %
   - 일일 입력 건수
   - 피드백 응답율

4. 비용 효율
   - OCR API 비용 / 입력 건수
   - 인프라 비용 / 활성 사용자
```

---

## 5. 종합 평가 및 권고사항

### 5.1 현재 단계 평가

✅ **강점**
- 명확한 사용자 니즈 정의 (타이핑 자동화 + 데이터 무결성)
- 성숙한 기술 스택 선택 (React, Node.js, PostgreSQL)
- 데이터베이스 기반 설계로 수식 깨짐 방지

⚠️ **보완 필요**
- OCR 신뢰도 강화 (정확도 92% 이상 필요)
- 대용량 처리 성능 최적화 (Response time ≤5초)
- 오프라인 모드 + 데이터 동기화 메커니즘
- 보안 및 규정 준수 (금융 데이터 취급)

🔴 **즉시 해결 필요**
- 보안 아키텍처 (역할 기반 접근 제어, 암호화)
- 데이터 백업 및 재해 복구 계획
- 성능 모니터링 인프라
- 에러 처리 및 페일오버 전략

### 5.2 우선순위별 권고

**Phase 1 (Weeks 1-4): 기초 구축**
- 보안 아키텍처 설계
- 핵심 API 설계 (Sales, Invoice, Reconciliation)
- 데이터베이스 스키마 최종 설계

**Phase 2 (Weeks 5-8): MVP 개발**
- Manager App 기본 기능
- OCR 파이프라인 (Google Vision API)
- Admin Dashboard 데이터 시각화

**Phase 3 (Weeks 9-12): 최적화 + 강화**
- OCR 성능 개선 (배치 처리, 캐싱)
- Statement Reconciliation 알고리즘
- 테스트 커버리지 ≥80%

**Phase 4 (Weeks 13-16): 배포 준비**
- 보안 감사 + 규정 준수 검증
- 사용자 테스트 (5~10개 지점)
- 모니터링 + 알람 설정
- 온보딩 자료 + 문서 작성

---

## 6. 다음 단계

1. **아키텍처 설계 검토** → `architecture-plan.md` 참조
2. **구현 로드맵 확정** → `implementation-roadmap.md` 참조
3. **테스트 전략 수립** → `testing-strategy.md` 참조
4. **이해관계자 승인** 획득

---

**문서 버전**: v1.0  
**최종 검토**: 2026-08-17
